# Python packages used by SignalRGB Sync System (tested with Python 3.14 and these versions)
pillow>=12.0            # reads album covers / screenshots        (tested 12.3.0)
winrt-runtime>=3.2      # Windows "now playing" media sessions    (tested 3.2.1)
winrt-Windows.Foundation>=3.2
winrt-Windows.Foundation.Collections>=3.2
winrt-Windows.Media.Control>=3.2
winrt-Windows.Storage.Streams>=3.2
pycaw>=20240000         # Sonar channel meters                    (tested 20251023)
comtypes>=1.4
psutil>=7.0             # process names                           (tested 7.2.2)
mss>=10.0               # screen capture                          (tested 10.2.0)
numpy>=2.0              # audio mixing                            (tested 2.5.3)
PyAudioWPatch>=0.2.12   # audio capture/playback for the mixer    (tested 0.2.12.8)
pystray>=0.19           # tray icon                               (tested 0.19.5)
dxcam>=0.0.5            # GPU screen capture for Sync Screen Dominant (optional; mss fallback)
